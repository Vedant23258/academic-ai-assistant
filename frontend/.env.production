# Production Environment Variables
# Backend API URL - Add your backend URL here when deployed
VITE_API_URL=http://localhost:5000

# For now, the app will work in frontend-only mode
# Backend features will be disabled until backend is deployed
